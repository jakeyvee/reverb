{
  "name": "@reverb/jobs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "trigger.dev dev",
    "build": "trigger.dev deploy --skip-deploy",
    "deploy": "trigger.dev deploy",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(jobs) no tests yet\"",
    "clean": "rm -rf .trigger .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@reverb/ai": "workspace:*",
    "@reverb/db": "workspace:*",
    "@reverb/domain": "workspace:*",
    "@reverb/srs": "workspace:*",
    "@trigger.dev/sdk": "^3.3.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@reverb/config": "workspace:*",
    "@trigger.dev/cli": "^3.3.0",
    "@types/node": "^22.10.5",
    "eslint": "^9.17.0",
    "typescript": "^5.6.3"
  }
}
